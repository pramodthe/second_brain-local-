<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application>
        <!-- Development-only command-line diagnostics; excluded from release builds. -->
        <receiver
            android:name=".probe.BrainProbeReceiver"
            android:exported="true"
            tools:ignore="ExportedReceiver">
            <intent-filter>
                <action android:name="com.secondbrain.app.PROBE" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
